class Test {
	def main() : int {
		writeln(len);
		return 0;
	}
}

class Test{
	var test2 : string;
	def testMethod(f1: int , f2: int) : int {
		if( new int[10] = 1) then{
			test2 = 2 + 3 || 4 / 5;
			2+3;
		}
		(a=b);
		a = new int[10];
		a = b = c;
		a = --(-(!(4 * 8)) + 8);
		
		return ((a.b()).c());#gfhfhfh
	}
}